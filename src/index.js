const genDiff = (filepath1, filepath2, format = 'stylish') => {
  // Временная заглушка, чтобы проверить, что аргументы дошли
  return `Comparing ${filepath1} and ${filepath2} using ${format} format`
}

export default genDiff
